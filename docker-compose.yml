version: '3.8'

services:
  claw-agent:
    build: .
    container_name: local-claw-agent
    restart: unless-stopped
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - DISCORD_TOKEN=${DISCORD_TOKEN}